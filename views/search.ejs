<div class="container-fluid container-fluid-custom pb-5 mb-5">
  <div class="row">
    <% if(searchResult != '') { %> <% searchResult.forEach((note, index)=>{ %>

    <a href="/dashboard/item/<%= note._id %>">
      <h4><%= note.title %></h4>
    </a>

    <% }) %> <% } else { %>
    <p>No Notes found.</p>
    <% } %>
  </div>
</div>
